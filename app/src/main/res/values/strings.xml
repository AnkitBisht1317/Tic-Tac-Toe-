<resources>
    <string name="app_name">Tic Tac Toe</string>

    <!-- Tic-Tac-Toe Cell Style -->
    <style name="TicTacToeCell">
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">100dp</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:background">@drawable/round_back_dark_blue</item>
    </style>

    <!-- Custom AlertDialog Style -->
    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_background</item> <!-- Rounded Background -->
        <item name="android:textColorPrimary">#FFFFFF</item> <!-- Title & Text Color -->
        <item name="android:buttonBarPositiveButtonStyle">@style/DialogButton</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/DialogButton</item>
    </style>

    <!-- Transparent Dialog Button Style -->
    <style name="DialogButton" parent="Widget.AppCompat.Button">
        <item name="android:background">#00000000</item> <!-- Transparent Background -->
        <item name="android:textColor">#FFFFFF</item> <!-- White Text Color -->
    </style>
</resources>
